apiVersion: 1

providers:
  - name: ExecRelay
    orgId: 1
    folder: ExecRelay
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
